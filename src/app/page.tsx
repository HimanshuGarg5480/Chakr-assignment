import { Commandbar } from "@/appComponents/Commandbar";
import Sidebar from "@/appComponents/Sidebar";
import React from "react";

const page = () => {

  return (
    <main className="bg-slate-600 w-full h-screen text-3xl font-semibold text-slate-300 text-center">
      Chakr Assignment
    </main>
  );
};

export default page;
